#include<stdio.h>
void main(){
    int salary, basic, hra,da, allow,pf;
    char grade;
    printf("Enter the  basic salary:");
    scanf("%d",&basic);
    printf("Enter the grade(only an upper case character):");
    scanf("%c",&grade);
    hra= 0.2*basic;
    da= 0.5*basic;
    pf= 0.11*basic;
    if(grade='A'){
        
    }

}